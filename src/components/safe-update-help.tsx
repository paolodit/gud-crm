export function SafeUpdateHelp() {
  return <details className="safe-update-help"><summary>How to set up safe updates</summary><ol><li>Ask your server administrator to create a backup endpoint that exports this instance’s database to private, durable storage. Test restoring a backup before relying on it.</li><li>Set <code>GUD_BACKUP_WEBHOOK_URL</code> in your server’s private environment (CapRover → App Configs → Environment Variables). It receives a POST and must return success only after the backup finishes, within 120 seconds.</li><li>Set <code>GUD_DEPLOY_WEBHOOK_URL</code> to a POST endpoint that deploys an approved release to this instance. Protect both endpoints with unguessable credentials and HTTPS; never publish their URLs.</li><li>Redeploy or restart the app, then check both indicators say Connected. GUD calls the backup first and stops if it fails. A successful HTTP response is the endpoint’s acknowledgement, not a restore test.</li></ol><p>No automation? You can still back up the database manually, verify the export, then deploy through CapRover. Never use a dummy backup endpoint to enable this button.</p><a href="https://github.com/paolodit/gud-crm/blob/main/docs/CAPROVER.md" target="_blank" rel="noreferrer">CapRover deployment guide</a></details>;
}
